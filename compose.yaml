services:
  backend:
    image: mirco-blog-backend
    volumes:
      - .:/micro-blog-backend/
    ports:
      - "5000:4500"
    networks:
      - blog-network

networks:
  blog-network:
    driver: bridge

